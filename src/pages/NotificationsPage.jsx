import { Bell } from "lucide-react";
import PlaceholderPage from "../components/common/PlaceholderPage";

export default function NotificationsPage() {
  return (
    <PlaceholderPage
      title="Notifications"
      description="Show likes, new posts, renewals, purchases and direct message alerts on this screen."
      icon={Bell}
    />
  );
}