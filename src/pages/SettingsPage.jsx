import { Settings } from "lucide-react";
import PlaceholderPage from "../components/common/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Configure privacy, billing, payout setup, password management and profile preferences."
      icon={Settings}
    />
  );
}